# Same allowlist as the ck-teaching repo — these packages need their install
# scripts to run (native bindings / platform binaries).
allowBuilds:
  '@parcel/watcher': true
  better-sqlite3: true
  esbuild: true
  sharp: true
  vue-demi: true
